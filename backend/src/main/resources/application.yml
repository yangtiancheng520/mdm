server:
  port: 3000

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/vue_admin?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
