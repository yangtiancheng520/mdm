========================================
Maven 环境变量配置完成
========================================

已设置的变量：
- MAVEN_HOME = D:\maven\mvn
- PATH 添加了 ;D:\maven\mvn\bin

生效方式：
1. 关闭所有命令行窗口
2. 重新打开新的命令行窗口
3. 运行 mvn -version 验证

现在可以直接使用：
- mvn spring-boot:run
- mvn clean install
等命令

========================================
